# 验证记录 · 0.4.0 · 2026-09-13

`npm test`：74项通过，0失败。

| 文件 | 数量 | 范围 |
|---|---:|---|
| engine.test.cjs | 16 | 核心优先级、预算、暂停、防重复 |
| catalog.test.cjs | 15 | 公开演出链接、日期与计划解析 |
| collector.test.cjs | 3 | 固定DOM样本采集 |
| background.test.cjs | 16 | 模拟Chrome API：提醒、消息权限、未适配渠道拒绝启动、损坏购买记录保留 |
| purchase.test.cjs | 24 | 测试适配器：并发任务、结算检查、持久化失败、停止竞态、超时、查单关联、备选、重启恢复 |

所有JS/CJS脚本通过语法检查，JSON配置可解析，Git差异空白检查通过。

购买测试使用显式的 `fixture-only` 适配器，未连接大麦，不是实际抢票或锁票测试。没有注册真实下单适配器，界面“已验证渠道数量”为0。

本轮未执行真实浏览器的新界面交互、多标签购买或系统通知验收，未创建真实订单、未付款；GitHub Actions远程运行结果未核实。此前本地界面预览曾被远程浏览器访问限制阻断，不据此判断用户本机是否可用。

测试提供已列场景的回归证据，不能保证所有竞态、页面变体或服务器行为都已覆盖，更不能保证抢到票。
