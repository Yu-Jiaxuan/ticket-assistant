# 验证记录 · 2026-09-12

版本：0.1.0（流程验证原型）。

## 已执行

- `npm test`：11 项测试全部通过，0 失败。
- `node --check`：扩展观察器、弹窗逻辑与模拟控制器通过语法检查。
- 核心用例：优先级、明确售罄切备选、未知状态暂停、开售/截止时间、验证码/登录/排队/结构改变/已有订单、价格与数量、提交前记录、进程重启重复提交保护、未知响应、订单目标匹配、停止操作、错误配置、已付款记录阻止重购。

## 尚未执行成功

- 浏览器端演示与扩展加载验收：环境缺少 Chromium；尝试安装时下载超时，因此未运行浏览器场景。`tests/browser.cjs` 提供 10 个可重跑场景，但这些场景不能记为通过。
- 已登录大麦页面观察：未执行。
- 真实大麦选票、结算、锁票、付款：未执行；当前未实现真实自动下单适配器。
- GitHub Actions：已提供配置文件，尚未在 GitHub 运行。

这不是生产发布验收报告。单元测试只证明所测决策逻辑，不证明大麦当前流程可自动化，不证明购票成功率。
