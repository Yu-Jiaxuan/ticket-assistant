# 验证记录 · 0.3.0 · 2026-09-12

## 本轮执行通过

`npm test`：47项通过，0失败。

| 测试文件 | 数量 | 覆盖 |
|---|---:|---|
| engine.test.cjs | 16 | 优先级、预算、验证暂停、防重复、损坏日志、配置不可变、提案失效、缺失结果 |
| catalog.test.cjs | 15 | 官方入口、北京时间、日期校验、列表上限、计划校验、多轮开售歧义、项目切换规则 |
| background.test.cjs | 13 | 模拟Chrome API：重复保存、并发、消息来源、提醒恢复、提前触发、通知失败/重试、中断、诊断脱敏 |
| collector.test.cjs | 3 | 固定DOM样本：单个开售时间、多轮冲突、重复状态节点 |

相关JS/CJS脚本通过 `node --check`，manifest/package JSON可解析，git差异空白检查通过。测试不依赖网络、真实库存或账号。

项目切换测试验证的是纯规则；采集脚本测试使用固定的最小DOM样本；后台使用模拟API。这些都不能替代真实浏览器交互验收。

## 之前的浏览器观察与限制

同日此前打开大麦搜索页持续显示“努力加载中”、共0个商品，未取得可用演出卡片。不能据此判断全站没有演出或认定是风控。本轮没有把这个结果当作真实页面适配证据。

此前远程浏览器尝试打开新UI本地地址时返回 `net::ERR_BLOCKED_BY_CLIENT`，界面视觉与交互验收没有完成。本轮未重复该受限路径。

## 尚未验收

- 用户浏览器安装扩展、正常加载官方页面的真实采集。
- 0.3界面实际交互、小屏布局、系统通知展示/点击和真实浏览器重启恢复。
- 真实大麦选票、结算、提交、锁票核对、付款：均未实现或执行。
- GitHub Actions本轮远程运行结果未核实；上述47项为本地执行结果。

测试通过不等于大麦自动抢票可用，也不代表已在操作系统上收到提醒。
